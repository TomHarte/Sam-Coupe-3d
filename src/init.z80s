;
;	init.z80s
;	---------
;
;	The code in this file is called exactly once, immediately after the game has been loaded.
;

Init:

;
;	Test code to set a specific start position for the player;
;	kind of pointless since we get to pick the memory image at
;	startup and hence disabled.
;
;		ld hl, -0x200
;		ld (PlayerInfoBlock+INFO_POSZ), hl

	ret